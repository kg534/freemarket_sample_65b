.contents
  .divise--header
    .devise--header__logo
      = image_tag 'logo.png'
  .devise--main
    .devise--main__container
      .devise--main__container__head
        %h2 会員情報入力
      .devise--main__container__form
        = form_for(@user, url: user_registration_path) do |f|
          .form--group
            = f.label "ニックネーム"
            %span 必須
            = f.text_field :nickname, placeholder:  "例）フリマ次郎", class: 'register--nickname'
            .error_explanation-user
              = @user.errors.full_messages_for(:nickname).first
          .form--group
            = f.label "メールアドレス"
            %span 必須
            = f.email_field :email, autofocus: true, autocomplete: "email", placeholder: "PC・携帯どちらでも可", class: 'register--email'
            .error_explanation-user
              = @user.errors.full_messages_for(:email).first
          .form--group
            = f.label "パスワード"
            %span 必須
            = f.password_field :password, autocomplete: "new-password", placeholder: "7文字以上の半角英数字", class: 'register--password'
            .error_explanation-user
              = @user.errors.full_messages_for(:password).first
          %p.form--note ※英字と数字の両方を含めて設定してください
          .form--group
            = f.label "パスワード(確認)"
            %span 必須
            = f.password_field :password_confirmation, autocomplete: "new-password", placeholder: "確認のため再入力", class: 'register--password'
            .error_explanation-user
              = @user.errors.full_messages_for(:password_confirmation).first
          .form--group
            %h3 本人確認
            %p.form--description 安心・安全にご利用いただくために、お客さまの本人情報の登録にご協力ください。他のお客さまに公開されることはありません。
          .form--group
            = f.label "お名前(全角)"
            %span 必須
            %br
            = f.text_field :last_name, placeholder: "例）田中", class: 'register--family_name__kanji'
            = f.text_field :first_name, placeholder: "例）太郎", class: 'register--first_name__kanji'
            .error_explanation-user
              = @user.errors.full_messages_for(:last_name).first
              = simple_format(@user.errors.full_messages_for(:first_name).first)
          .form--group
            = f.label "お名前カナ(全角)"
            %span 必須
            %br
            = f.text_field :j_last_name, placeholder: "例）タナカ", class: 'register--family_name__kana'
            = f.text_field :j_first_name, placeholder: "例）タロウ", class: 'register--first_name__kana'
            .error_explanation-user
              = @user.errors.full_messages_for(:j_last_name).first
              = simple_format(@user.errors.full_messages_for(:j_first_name).first)
          .form--group
            = f.label "生年月日"
            %span 必須
            %br
            = f.date_select :birthday, {with_css_classes: 'register--birthday', prompt: '--', use_month_numbers: true, start_year: 1900,  end_year: (Time.now.year), default: Date.new(1990, 1, 1), date_separator: "/ ", order: [:year, :month, :day]}
            .error_explanation-user
              = @user.errors.full_messages_for(:birthday).first
          .form--group
            = f.label "電話番号"
            %span.default 任意
            %br
            = f.text_field :phone_number, placeholder: '例）09012345678', class: 'register--phone__number'
            .error_explanation-user
              = @user.errors.full_messages_for(:phone_number).first
          .form--group
            %p.form--submit__note ※本人情報は正しく入力して下さい。会員登録後、修正するにはお時間を頂く場合があります
            = f.submit "次へ進む", class: 'register--submit__btn'
  .devise--footer
    %nav
      %ul 
        %li 
          プライバシーポリシー
        %li 
          FURIMA利用規約
        %li 
          特定商取引に関する表記   
    .devise--footer_logo
      = image_tag 'logo-white.png'
      %p.logo--link @FURIMA